<div class="well well-sm">
  <a href="http://ja.wikipedia.org/wiki/Markdown">Markdown記法</a>で入力してください。タグを複数設定する場合は半角スペースで区切ってください。
</div>

<%= form_for(@report) do |f| %>
  <div class="row">
    <div class="col-md-6">
      <% if @report.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>

          <ul>
          <% @report.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field form-group">
        <%= f.text_field :tag, class: "form-control typeahead", placeholder:"タグ", autocomplete: "off" %>
      </div>
      <div class="field form-group">
        <%= f.text_field :title, id: "title", class: "form-control", placeholder:"タイトル" %>
      </div>
      <div class="field form-group">
        <%= f.text_area :content, id: "content", class: "form-control", placeholder:"内容", rows: 20 %>
      </div>
      <div>
        <% @report.attachments.each do |a| %>
          <%= a.name %>
          <%= link_to '削除', a, method: :delete, data: { confirm: '削除しますか?' }, class: "btn btn-warning btn-sm" %>
          <br>
        <% end %>
      </div>
      <br>
      <div class="field form-group">
        <%= f.label :attachments, '添付ファイル' %> <span>(複数ファイル選択できます)</span>
        <%= f.file_field :attachments, id: "attachments", multiple: true %>
      </div>
      <div class="field form-group form-inline">
				<%= f.hidden_field :user_id, :value => current_user.id %>
      </div>
    </div>

    <div class="col-md-6">
      <div id="title-view"></div>
      <div id="content-view" class="content-view-edit"></div>
    </div>
  </div>
  <hr>
  <div class="actions">
    <%= link_to '戻る', reports_path, class: "btn btn-default" %>
		<% unless name == '更新' && @report.user_id != current_user.id %> 
			<%= submit_tag name, class: "btn btn-primary" %>
		<% end %>
  </div>
<% end %>